@{
    ViewBag.Title = "Monzo Bank Login";
}

<div class="content-shadow">
    <div class="content-inner dark-bg">
        <h1 class="text-center">@ViewBag.Title</h1>
        @Html.ActionLink("Login with Monzo", "Login", new { }, new { @class = "btn btn-lg btn-primary", role = "button" })
    </div>
</div>
